import React from "react";
import withHeader from "../Components/withHeader";
import ListDetail from "../Components/ListDetail";

const ListDetails = () => {

  return (
    <ListDetail/>
  );
};
export default withHeader(ListDetails);